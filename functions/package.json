{
  "name": "functions",
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "tsup",
    "build:watch": "tsup --watch",
    "serve": "npm run build && firebase emulators:start --import=./emulator-data --export-on-exit",
    "serve:fresh": "npm run build && firebase emulators:start --export-on-exit=./emulator-data",
    "serve:functions": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "npm run build && ESLINT_USE_FLAT_CONFIG=false firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "GCLOUD_PROJECT=demo-builtmode FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 jest src/tests/unit",
    "test:integration": "GCLOUD_PROJECT=demo-builtmode FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 jest --runInBand src/tests/integration",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "dayjs": "^1.11.19",
    "dayjs-timezone-iana-plugin": "^0.2.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5"
  },
  "type": "module",
  "devDependencies": {
    "@jest/globals": "29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^25.3.3",
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "firebase-functions-test": "^3.4.1",
    "jest": "29.7.0",
    "ts-jest": "^29.4.6",
    "ts-node": "10.9.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "private": true,
  "version": "1.0.0"
}
